name: CI/CD Pipeline

on:
  push:
    branches:
      - '**'
    tags:
      - '*'

env:
  IMAGE_NAME: maltsevvm/test_app
  TAG: ${{ github.ref_name }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # Клонирование репозитория
      - name: Checkout repository
        uses: actions/checkout@v3

      # Установка Nginx для проверки конфигурации
      - name: Install Nginx
        run: |
          sudo apt-get update
          sudo apt-get install -y nginx

      # Проверка синтаксиса конфигурации Nginx
      - name: Validate Nginx Configuration
        run: |
          echo "Validating Nginx configuration..."
          sudo nginx -t -c $GITHUB_WORKSPACE/nginx.conf

      # Проверка наличия Dockerfile
      - name: Check Dockerfile exists
        run: test -f Dockerfile

      # Установка Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # Вход в DockerHub
      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Сборка и отправка Docker-образа
      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ env.TAG }}
          cache-from: type=registry,ref=${{ env.IMAGE_NAME }}:cache
          cache-to: type=registry,ref=${{ env.IMAGE_NAME }}:cache,mode=max

      # Тестирование доступности сайта
      - name: Test Site Availability
        run: |
          echo "Testing site availability..."
          docker run -d --name test_container -p 8080:80 ${{ env.IMAGE_NAME }}:${{ env.TAG }}
          sleep 5
          curl -I http://localhost:8080
          docker stop test_container
          docker rm test_container

      # Уведомление об успешной сборке
      - name: Notify Success
        if: success()
        uses: 8398a7/action-slack@v3
        with:
          status: success
          fields: repo,ref,author,commit
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

  deploy:
    if: startsWith(github.ref, 'refs/tags/')
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      # Клонирование репозитория
      - name: Checkout repository
        uses: actions/checkout@v3

      # Установка kubectl
      - name: Set up kubectl
        uses: azure/setup-kubectl@v4

      # Настройка Kubeconfig
      - name: Set up Kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG }}" > ~/.kube/config

      # Проверка доступности Kubernetes кластера
      - name: Verify Kubernetes connection
        run: |
          kubectl cluster-info
          echo "Kubernetes cluster is accessible."

      # Деплой Docker-образа в Kubernetes
      - name: Deploy to Kubernetes
        run: |
          # Verify that the deployment exists in the cluster
          if kubectl get deployment my-deployment -n default; then
            kubectl set image deployment/my-deployment my-container=${{ env.IMAGE_NAME }}:${{ env.TAG }} --record -n default
            kubectl rollout status deployment/my-deployment -n default
          else
            echo "Deployment 'my-deployment' not found in the 'default' namespace. Please ensure it exists or provide the correct name and namespace."
            exit 1
          fi

      # Проверка успешности деплоя
      - name: Verify deployment
        run: |
          kubectl get pods -n default
          echo "Deployment with tag ${{ env.TAG }} successfully rolled out."

      # Уведомление об успешном деплое
      - name: Notify Deployment Success
        if: success()
        uses: 8398a7/action-slack@v3
        with:
          status: success
          fields: repo,ref,author,commit
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
